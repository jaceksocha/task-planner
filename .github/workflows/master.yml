name: Master CI/CD

on:
  push:
    branches: [master, main]

jobs:
  lint-test-build:
    name: Lint, Test & Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: ".nvmrc"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Run linter
        run: npm run lint

      - name: Run unit tests
        run: npm test -- --run

      - name: Build project
        run: npm run build
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}

  # Optional: Add E2E tests (requires test user setup)
  # e2e-tests:
  #   name: E2E Tests
  #   runs-on: ubuntu-latest
  #   needs: lint-test-build
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version-file: ".nvmrc"
  #         cache: "npm"
  #     - run: npm ci
  #     - name: Install Playwright browsers
  #       run: npx playwright install chromium
  #     - name: Run E2E tests
  #       run: npm run test:e2e
  #       env:
  #         E2E_USERNAME: ${{ secrets.E2E_USERNAME }}
  #         E2E_PASSWORD: ${{ secrets.E2E_PASSWORD }}
  #         SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
  #         SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}

  # Deployment is handled by Cloudflare Pages automatic deployments
  # Or uncomment below for manual deploy step
  # deploy:
  #   name: Deploy to Cloudflare Pages
  #   runs-on: ubuntu-latest
  #   needs: lint-test-build
  #   if: github.ref == 'refs/heads/master' || github.ref == 'refs/heads/main'
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version-file: ".nvmrc"
  #         cache: "npm"
  #     - run: npm ci
  #     - run: npm run build
  #       env:
  #         SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
  #         SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
  #         OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
  #     - name: Deploy to Cloudflare Pages
  #       uses: cloudflare/wrangler-action@v3
  #       with:
  #         apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
  #         accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
  #         command: pages deploy dist --project-name=task-planner
